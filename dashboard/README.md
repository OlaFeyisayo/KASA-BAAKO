# Dashboard

Step 9: web page for MTN agents — list cases, view structured summary + original
audio, update status, filter by status/category. Built with React + Vite + Tailwind.

Currently uses sample/fake case data (`src/data/sampleCases.js`) — not yet
connected to the real backend.

## Run locally

```
npm install
npm run dev
```
